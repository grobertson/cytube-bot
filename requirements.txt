# Core dependencies for CyTube bot library
websockets>=12.0
requests>=2.32.3

# Web status server
Flask>=3.0.0

# Optional dependencies
PySocks>=1.7.1  # For proxy support
PyYAML>=6.0    # For YAML config files (optional, JSON still works)

# Bot-specific dependencies
markovify>=0.9.4  # For markov bot
blessed>=1.20.0   # For TUI bot

# Development dependencies (optional)
pytest>=8.2.2
pytest-mock>=3.14.0
coverage>=7.5.4
